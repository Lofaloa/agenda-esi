<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="agenda_esi.list">
      <field name="name">agenda_esi list</field>
      <field name="model">agenda_esi.agenda_esi</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="agenda_esi.action_window">
      <field name="name">agenda_esi window</field>
      <field name="res_model">agenda_esi.agenda_esi</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="agenda_esi.action_server">
      <field name="name">agenda_esi server</field>
      <field name="model_id" ref="model_agenda_esi_agenda_esi"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="agenda_esi" id="agenda_esi.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="agenda_esi.menu_1" parent="agenda_esi.menu_root"/>
    <menuitem name="Menu 2" id="agenda_esi.menu_2" parent="agenda_esi.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="agenda_esi.menu_1_list" parent="agenda_esi.menu_1"
              action="agenda_esi.action_window"/>
    <menuitem name="Server to list" id="agenda_esi" parent="agenda_esi.menu_2"
              action="agenda_esi.action_server"/>
    -->
  </data>
</odoo>